title: PKM
description: >-
  Persistent URI namespace at https://w3id.org/pkm with Linked Data resources on
  Personal Knowledge Management (PKM) for the Knowledge System Architecture (KSA) project.
url: https://w3id.org/pkm
theme: jekyll-theme-primer

# Build machinery — never published, so it can never become a resolvable URI.
exclude:
  - scripts/
  - generators/
  - schema/
  - generated/  # LinkML dev artifacts; derived, not citable
  - reports/    # validation output, regenerated on demand
  - vocab/src/  # SKOS Editor exports; the published artifact is vocab/pkm-vocab.ttl
  - z/          # scratch
  - .venv/
  - Makefile
  # Local site build only; Pages resolves its own gems and never reads these.
  # Named explicitly because Jekyll 3 replaces its own default excludes with
  # this list rather than adding to it -- without these, `vendor/bundle` is
  # copied into the built site and Gemfile publishes as a URI.
  - Gemfile
  - Gemfile.lock
  - vendor/
  - .bundle/
  - README.md   # index.md is the site index; README is for the GitHub repo page
